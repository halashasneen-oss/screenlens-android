<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.ScreenLens.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:minHeight="120dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/toolIcon"
            android:layout_width="@dimen/icon_size_lg"
            android:layout_height="@dimen/icon_size_lg"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_nav_scan" />

        <TextView
            android:id="@+id/toolTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_sm"
            android:textAppearance="@style/TextAppearance.ScreenLens.Body"
            android:textStyle="bold"
            tools:text="Screen Scanner" />

        <TextView
            android:id="@+id/toolDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_2xs"
            android:textAppearance="@style/TextAppearance.ScreenLens.Caption"
            tools:text="Capture and read text from your current screen." />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
